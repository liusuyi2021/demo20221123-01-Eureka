server:
  port: 7003
spring:
  application:
    name: eureka-server-7003
ribbon:
  eureka:
    enabled: true
eureka:
  client:
    fetch-registry: false #false ????????????
    register-with-eureka: false #false ??????????????????????????????????
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/ #??? ???? Eureka Server ??? 7003 ? 7003 ??????????? Eureka Server ??
  # ??     defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka
  instance:
    hostname: eureka7003.com
  server:
    enable-self-preservation: true
    eviction-interval-timer-in-ms: 10000




